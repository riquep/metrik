# URL do backend do dev_harness (POST /dev/parse).
# Sem essa variável, o frontend usa http://localhost:8000 (dev local).
# Em produção (Vercel), configure VITE_BACKEND_URL nas Environment
# Variables do projeto metrik_front com a URL do projeto metrik_api.
VITE_BACKEND_URL=http://localhost:8000
